# How to use

## Installation

```bash
```

## Authentication

- run `x auth` 
- after authorizing the app
- run `x auth --verify` or `x auth -v`

## Posting tweets

`x -t "tweet text"` or `x --tweet "tweet text"`